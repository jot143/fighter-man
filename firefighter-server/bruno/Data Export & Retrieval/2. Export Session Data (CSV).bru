meta {
  name: Export Session Data (CSV)
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/sessions/{{session_id}}/export?format=csv&include_raw=false
  body: none
  auth: none
}

params:query {
  format: csv
  include_raw: false
}

docs {
  Export session data in CSV format without raw data.
}
