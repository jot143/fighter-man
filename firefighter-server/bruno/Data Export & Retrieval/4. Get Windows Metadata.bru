meta {
  name: Get Windows Metadata
  type: http
  seq: 4
}

get {
  url: {{base_url}}/api/sessions/{{session_id}}/windows
  body: none
  auth: none
}

docs {
  Get all windows metadata for a session (lightweight).

  Returns window metadata WITHOUT raw_data field.
  Optimized for timeline rendering.
}
