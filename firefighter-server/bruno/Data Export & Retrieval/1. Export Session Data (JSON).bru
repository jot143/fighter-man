meta {
  name: Export Session Data (JSON)
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/sessions/{{session_id}}/export?format=json&include_raw=true
  body: none
  auth: none
}

params:query {
  format: json
  include_raw: true
}

docs {
  Export session data for annotation or ML training.

  **Query Parameters:**
  - `format`: 'json' or 'csv' (default: json)
  - `include_raw`: true/false - include raw sensor data

  Returns session metadata and windows in requested format.
}
