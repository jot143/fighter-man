meta {
  name: Get Replay Data
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/sessions/{{session_id}}/replay?offset=0&limit=20
  body: none
  auth: none
}

params:query {
  offset: 0
  limit: 20
}

docs {
  Get windows for replay with pagination.

  Optimized for smooth playback with window batching.
  Returns windows with raw_data included.

  **Query Parameters:**
  - `offset`: Starting position (default: 0)
  - `limit`: Number of windows (default: 20, max: 100)
}
