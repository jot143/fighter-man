meta {
  name: Delete Session
  type: http
  seq: 6
}

delete {
  url: {{base_url}}/api/sessions/{{session_id}}
  body: none
  auth: none
}

docs {
  Delete a session and all its associated data.

  Removes data from both PostgreSQL and Qdrant.
  Returns count of deleted windows.
}
