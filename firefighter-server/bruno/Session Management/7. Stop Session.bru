meta {
  name: Stop Session
  type: http
  seq: 7
}

post {
  url: {{base_url}}/api/sessions/{{session_id}}/stop
  body: none
  auth: none
}

docs {
  Stop the current recording session.

  Flushes remaining data from vector store.
  Broadcasts `session_stopped` event to Socket.IO clients.
}
