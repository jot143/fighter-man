meta {
  name: Stream Video
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/sessions/{{session_id}}/video
  body: none
  auth: none
}

docs {
  Stream video file for a session.

  Supports HTTP Range requests for seeking/streaming.
  Returns video/webm MIME type.
  Returns 206 Partial Content for range requests.

  **Optional Header:**
  - `Range: bytes=0-` for partial content requests
}
