meta {
  name: Upload Video
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/sessions/{{session_id}}/upload-video
  body: multipartForm
  auth: none
}

body:multipart-form {
  video: @file()
}

docs {
  Upload video file for a session.

  Multipart/form-data with 'video' file.
  Stores as .webm format with session_id as filename.
  Updates session record with video metadata.

  **Note:** Select a video file using the file picker in Bruno.
}
